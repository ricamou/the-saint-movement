# CommerceHub Mercado Livre Sales Ready

Versão focada em começar vendas no Mercado Livre.

Testes: `/api/health`, `/`, `/setup`, `/supabase`, `/api/foundation/seed`, `/new-product`, `/products`, `/mercado-livre`, `/api/mercadolivre/me`, `/api/ml/items`, `/api/ml/orders`, `/sell-flow`.
